class Solution {
public:
    bool isPalindrome(int x) {
        if(x < 0) return false;
        
        long long int res=x,rev=0;
        while(x)
        {
            int ld=x%10;
            rev=rev*10+ld;
            x/=10;
        }
        
        return (rev==res);
    }
};
